Readme:

- There are Classes for circularShifter,UpperCaseConvertor, Alphabetizer, Output
- The Events are Add, Delete, Print, Quit. Events take in lineStorage's instance
- The input filter relies on IInput Interface's objects: ConsoleInput, and FileInput which can be specified by user. Hence Strategy pattern is in use. 
- The UML diagram can be found at the home Directory
- The artifacts are in out
- Interactive shell opens up as soon as user inputs 2 for console option. 
- Default file input/output triggered as soon as user hits 1 for file option

-Source code is in src/